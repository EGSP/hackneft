# Файл базы SQLite. Относительный путь отсчитывается от каталога services/platform.
# PLATFORM_DATABASE_PATH=data/platform.db

# Адрес ИИ-сервиса. По нему платформа передаёт запросы страницы «Сессии»
# (ai_sessions.py); без ИИ-сервиса остальная платформа работает, а страница сообщает
# о его недоступности.
# AI_SERVICE_URL=http://localhost:8100
