from hackneft_platform.events import SensorDataCreated, dispatcher


# Подписчик диспетчера: место для будущей реакции ИИ-агента на новое показание датчика.
# Тело пустое намеренно. На текущем этапе платформа к ИИ-сервису не обращается и не
# зависит от него ни одной строкой кода; регистрация подписчика сохранена, чтобы точка
# подключения была очевидна. Когда реакция агента понадобится, она пишется здесь, внутри
# подписчика, а не в эндпоинте приёма данных: адрес ИИ-сервиса отдаёт config.ai_service_url().
@dispatcher.on()
async def notify_ai(event: SensorDataCreated) -> None:
    """Обработка события пока не выполняется."""
