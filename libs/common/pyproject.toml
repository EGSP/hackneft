[project]
name = "hackneft-common"
version = "0.1.0"
description = "Общий код: модели данных, конфигурация, служебные утилиты"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hackneft_common"]
